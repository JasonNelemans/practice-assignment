import { createGlobalStyle } from 'styled-components/macro';

export default createGlobalStyle`
    body {
        margin: 0;
        padding: 0;
        height: 100vh;
        width: 100vw;
    }
`;
